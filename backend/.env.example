# Supabase PostgreSQL connection string
DATABASE_URL=postgresql://[user]:[password]@[host]:[port]/[database]

# Example (do NOT use in production):
# DATABASE_URL=postgresql://postgres:password@localhost:5432/campaign_tracker
